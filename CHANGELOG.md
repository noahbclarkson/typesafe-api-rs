# Changelog

All notable changes are recorded here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and this project
follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Entries below the next heading are generated by `release-plz` from conventional
commits; add anything it cannot infer by hand.

## [Unreleased]

### Added

- The wire data model: `State`, `Entry`, `Question`, `Request`, `Response`,
  `Answer`, `Usage`, `ModelCard`, round-tripped against the payloads published
  in the API reference.
- Local request validation with per-field paths, tunable through `Limits`.
- An error type that reports status, request id, and whether a retry could help.
